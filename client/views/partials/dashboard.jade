row
    alert(ng-repeat='alert in errorHandler.alerts', type='{{alert.type}}', close='errorHandler.closeAlert($index)') {{alert.message}}

row
    h3 לבצע

    p(ng-show='todos.length == 0') אין משימות לביצוע

    table.table.dashboard-table
        tbody
            tr(ng-repeat="todo in todos")
                td#dashboard-checkbox-column
                    form
                        input(type='checkbox', ng-model='todo.done', ng-change='completeTodoAndRemove(todo, $index)')
                td
                    {{todo.toString()}}
                td
                    a.location-selector(ng-click='todo.view()')
                        i.glyphicon.glyphicon-zoom-in

row
    h3 פגישות קרובות

    p(ng-show='upcomingAppointments.length == 0') אין פגישות קרובות

    table.table.dashboard-table
        tbody
            tr(ng-repeat="appointment in upcomingAppointments | orderBy:'when'")
                td {{appointment.when | date:"EEEE, dd MMMM 'ב-' HH:mm"}} עם {{appointment.patient.name}}
                td
                    a.location-selector(href='patients/{{appointment.patient._id}}')
                        i.glyphicon.glyphicon-zoom-in